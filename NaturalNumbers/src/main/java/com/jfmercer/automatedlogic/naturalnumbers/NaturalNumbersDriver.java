package com.jfmercer.automatedlogic.naturalnumbers;

/**
 * Description: AutomatedLogicPuzzles
 *
 * @author: John Mercer
 * @version: AutomatedLogicPuzzles
 */

public class NaturalNumbersDriver
{
   public static void main(String[] args) {

      NaturalNumbers.calculateNumberTotal(10);
      NaturalNumbers.calculateNumberTotal(237);
      NaturalNumbers.calculateNumberTotal(129);
      NaturalNumbers.calculateNumberTotal(-841);
      NaturalNumbers.calculateNumberTotal(1028);
      NaturalNumbers.calculateNumberTotal(2048);
      NaturalNumbers.calculateNumberTotal(2);
      NaturalNumbers.calculateNumberTotal(56);
      NaturalNumbers.calculateNumberTotal(143);
      NaturalNumbers.calculateNumberTotal(537);
      NaturalNumbers.calculateNumberTotal(629);
      NaturalNumbers.calculateNumberTotal(-1166);
      NaturalNumbers.calculateNumberTotal(1789);
      NaturalNumbers.calculateNumberTotal('a');
      NaturalNumbers.calculateNumberTotal(349);
      NaturalNumbers.calculateNumberTotal(277);
      NaturalNumbers.calculateNumberTotal(4302);
      NaturalNumbers.calculateNumberTotal(1234);
      NaturalNumbers.calculateNumberTotal(51);
      NaturalNumbers.calculateNumberTotal('b');
      NaturalNumbers.calculateNumberTotal(987);
      NaturalNumbers.calculateNumberTotal(452);
      NaturalNumbers.calculateNumberTotal(612);
      NaturalNumbers.calculateNumberTotal(-10000);
   }
}
